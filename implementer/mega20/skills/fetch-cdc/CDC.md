# fetch - CDC

Source: MCP tools/list (1 tools)
Use mcp-call.js openSession() from a script; print answer only.

## misc
fetch(url*, max_length:integer, start_index:integer, raw:boolean) — Fetches a URL from the internet and optionally extracts its contents as markdown

## _index
fetch
