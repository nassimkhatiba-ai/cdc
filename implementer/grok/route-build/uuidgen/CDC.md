# uuidgen - CDC

Source: MCP tools/list (4 tools)
Prefer: node mcp-call.js --batch (ONE shell call). Answer: JSON.stringify once; never dump tool results.

## is
is_uuid(s*) — Validate UUID shape

## misc
v4() — Generate UUID v4
nil() — Nil UUID
batch(n*:integer) — Generate n UUIDs

## _index
batch
is_uuid
nil
v4
