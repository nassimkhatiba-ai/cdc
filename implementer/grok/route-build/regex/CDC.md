# regex - CDC

Source: MCP tools/list (4 tools)
Prefer: node mcp-call.js --batch (ONE shell call). Answer: JSON.stringify once; never dump tool results.

## find
find_all(pattern*, s*) — Find all matches

## misc
match(pattern*, s*) — Test regex
replace(pattern*, s*, with*) — Replace all
count(pattern*, s*) — Count matches

## _index
count
find_all
match
replace
