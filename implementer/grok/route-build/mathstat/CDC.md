# mathstat - CDC

Source: MCP tools/list (5 tools)
Prefer: node mcp-call.js --batch (ONE shell call). Answer: JSON.stringify once; never dump tool results.

## misc
sum(nums*:[]) — Sum array
mean(nums*:[]) — Mean
median(nums*:[]) — Median
stdev(nums*:[]) — Population stdev
percentile(nums*:[], p*:number) — Percentile p in 0..100

## _index
mean
median
percentile
stdev
sum
