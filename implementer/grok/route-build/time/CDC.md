# time - CDC

Source: MCP tools/list (2 tools)
Prefer: node mcp-call.js --batch (ONE shell call). Answer: JSON.stringify once; never dump tool results.

## convert
convert_time(source_timezone*, time*, target_timezone*) — Convert time between timezones

## get
get_current_time(timezone*) — Get current time in a specific timezone

## _index
convert_time
get_current_time
